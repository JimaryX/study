package Overview;

import java.io.Console;
import java.io.IOException;
import java.io.PrintWriter;
import java.nio.file.Paths;
import java.util.Arrays;
import java.util.Scanner;

public class Demo01 {
    public static void main(String[] args) {
//        int i = 1_000_000;  // 下划线不影响，会生成特定格式的int数值
//        int j = 0b10;        // 此为二进制的数
//        System.out.println(i);
//        System.out.println(j);

//        System.out.println(Math.floorMod(-9, 2)); // floorMod方法会一直获取一个正数的模值

//        System.out.println(1 << 35); // 左边为int时，右边得与32取模， 若左边为long时，右边得与64取模
//        System.out.println(1 << 3);

//        String astr = "hello!";
//        String bstr = astr;
//        System.out.println(astr == bstr); // 结果为true， astr与bstr共享相同的字符

//        System.out.printf("%1$d, %2$x", 159, 16); // %1$d 输出第一个参数的十进制  %2$x 输出第二个参数的十六进制

//            Scanner in = new Scanner(Paths.get("file.txt"), "utf-8");  // file.txt 的路径在StudyForFuture下
//            PrintWriter out = new PrintWriter("file.txt", "utf-8");
//            System.out.println(in.nextLine());

//        String[] strs = {"This", " is ", "a good ", "chance!"};
//        System.out.println(Arrays.toString(strs));    // Arrays.toString(args) 可以在打印数组时使用。二维数组用Arrays.deepTOString();

        //  有关数组的操作很多与Arrays相关，如拷贝数组的值，使用Arrays.copyOf() 方法。 此方法有两个参数，原数组和拷贝后的长度

    }
}
